import { CartView } from './cart-view';

export const metadata = {
  title: 'Your Cart — 801 Outlet',
  description: 'Review the items in your cart and proceed to checkout.',
};

export default function CartPage() {
  return <CartView />;
}
